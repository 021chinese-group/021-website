import styled from "styled-components"

export default styled.div`
  background-color: rgba(33, 99, 170, 0.2);
  border-radius: 5px;
  box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
  padding: 50px 5px;

  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-around;
`
